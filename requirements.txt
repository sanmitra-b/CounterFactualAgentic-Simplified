# Shared project dependencies (Layer 1 + Layer 2 + Layer 3)
# Install with: pip install -r requirements.txt

# Core utilities
python-dotenv>=0.19.0
requests>=2.28.0
numpy>=1.23.0
pandas>=1.5.0

# Layer 1 collectors
feedparser>=6.0.0
yfinance>=0.2.0

# Layer 2 NLP
torch>=2.1.0
transformers>=4.40.0

# Layer 3 LLM + schema validation
groq==0.10.0
pydantic>=2.7.0
regex>=2024.9.11,<2025.0.0

# Layer 4 counterfactual + causal graph + agent loop
networkx>=3.2.0
dowhy>=0.11.0
cloudpickle>=3.0.0

# Layer 6 PDF generation
reportlab>=4.0.0

# Compatibility pins (recommended for Counterfactual environment)
# These versions were validated together during development and pipeline runs
anyio==3.7.1
httpx==0.24.1
httpcore==0.17.3
h11==0.14.0
pyyaml>=6.0

# Optional development tools
pytest>=7.0.0
black>=22.0.0
